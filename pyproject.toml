[project]
name = "worldcup-predictor"
version = "1.0.0"
description = "World Cup match predictions using Football-Data.org and Polymarket"
requires-python = ">=3.13"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
